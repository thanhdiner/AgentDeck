# Prompt: Git Checkpoint Helper

You are a developer tooling engineer.

Implement a Git checkpoint helper.

Before running risky agent mode, app should offer:

1. Check git status
2. Show changed files
3. Create checkpoint commit
4. Continue without checkpoint only after explicit confirmation
5. Block risky run if folder is not a git repo unless user overrides

Output:

- UX flow
- Commands
- IPC implementation
- Error handling
- UI copy
